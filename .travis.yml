sudo: required
dist: trusty
addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

language: node_js
node_js:
  - "6"
  - "7"
  - "8"
cache:
  directories: node_modules
branches:
  only: master

before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
  - yarn
  - cd packages/@ngx-config/core
  - yarn
  - cd ../../..
  - cd packages/@ngx-config/http-loader
  - yarn
  - cd ../../..
  - cd packages/@ngx-config/fs-loader
  - yarn
  - cd ../../..
  - cd packages/@ngx-universal/config-loader
  - yarn
  - cd ../../..
  - cd packages/@ngx-config/merge-loader
  - yarn
  - cd ../../..
  - sleep 3
after_failure:
  - cat /home/travis/build/fulls1z3/ngx-config/npm-debug.log
after_success:
  - bash <(curl -s https://codecov.io/bash)

notifications:
  email: true
  webhooks:
    on_success: change  # options: [always|never|change] default: always
    on_failure: always  # options: [always|never|change] default: always
    on_start: never     # options: [always|never|change] default: always
